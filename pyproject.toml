[project]
name = "polymarket-board"
version = "0.1.0"
description = "Polymarket dashboard — terminal & browser"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "rich>=14.0.0",
    "aiofiles>=24.1.0",
]
